#<AdxTL>@(#)0.0.0.0 $Revision$
# Gestion du masque PDSYSQH

######################################################################################
## Clic droit Pièce sur la commande
######################################################################################
Subprog IB_NBSOH

  GBOUT2 = mess(98,197,1)
  If GPILNAV>1 Then
    # Si on vient de la gestion des objets, on empêche la consultation de la pièce
    If find("GESSOH",GNAVIG(1..GPILNAV)) Then GBOUT2="" Endif
  Endif
End

Subprog B2_NBSOH
Local Integer NOL

  If clalev([F:SOH])=0 : Local File SORDER [SOH] : Endif 
  Filter [F:SOH]
  GCONSULT = 2
  Local Char SAV_VCRNUM
  # On sauvegarde le VCRNUM, car si la pièce n'existe pas, ou ne peut être affichée
  # car filtre dans la gestion d'objet, la clé va être modifiée, et donc le VCRNUM aussi
  NOL = nolign-1
  SAV_VCRNUM = [M:PQO2]SOHNUM(NOL)
  If [M:PQO2]SOHCAT(NOL)<>4 Then
    Call OBJET("SOH",[M:PQO2]SOHNUM(NOL),"") From GOBJET
  Else
    Call OBJET("SOQ",[M:PQO2]SOHNUM(NOL),"") From GOBJET
  Endif
  [M:PQO2]SOHNUM(NOL) = SAV_VCRNUM 

  GCONSULT = 0
End


######################################################################################

