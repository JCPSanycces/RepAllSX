#<AdxTL>@(#)0.0.0.0 $Revision$
##########################################################################
# UTISTO14   Création du paramètre STOCLOPER                             #
##########################################################################
# DM 64203 - 2010-07-05 by CCC
##########################################################################
Local Char DOSSIER(30)

  DOSSIER = nomap
  #--- Issue 108760 by TS
  #Call SAIDOS(DOSSIER,"") From SAIDOS
  Call SAIDOSHIS(DOSSIER,"") From SAIDOSHIS
  #---
  If DOSSIER <> "" Then
    If clalev([F:ADS]) = 0 : Local File ADOSSIER [ADS] : Endif
    #--- Issue 108760 By TS
    #Read [ADS] DOSSIER=[L]DOSSIER : If fstat Raz [F:ADS] : Endif
    If func DOSSUB.FILL_ADS([L]DOSSIER)=0 : End : Endif
    #---
    Call TEMPON("O") From GESECRAN
    Call OUVRE_TRACE("Correction") From LECFIC
    Call MAJ_UTISTO14(DOSSIER)
    Call TEMPOFF     From GESECRAN
    Call FERME_TRACE From LECFIC
    Call LEC_TRACE From LECFIC
  Endif
End

##########################################################################
Subprog MAJ_UTISTO14(DOSSIER)
Value Char DOSSIER

Local Integer COMPTEUR
Local Char WOBJET(GLONAOB)
Local Integer WNBR
Local Integer B_OK

  Call ECR_TRACE("UTISTO14: Process Start on : "+DOSSIER,0) From GESECRAN
  #----- Ouverture des fichiers -----#
  Local File =DOSSIER + ".ADOVAL"  [ADW]
  Trbegin [ADW]
  Look [F:ADW]ADW0="";"";"STOCLOPER"
  If fstat
      Raz [F:ADW]
      [F:ADW]PARAM = "STOCLOPER"
      [F:ADW]CREDAT = date$
      [F:ADW]CREUSR = GUSER
      [F:ADW]VALEUR = "3"
      Write [F:ADW]
      If fstat
          Call ECR_TRACE("STOCLOPER: can't create parameter",1) From GESECRAN
      Endif
  Endif
  Commit
  Close Local File [ADW]
  Call ECR_TRACE("UTISTO14 : finish",0) From GESECRAN
End

########################################################################
# Pour exécution automatique  en gestion de patch                      #
########################################################################
Subprog PATCH(APPLI)
Value Char    APPLI

  If APPLI = GDOSX3 Then
    End
  Endif
  If clalev([F:ADS]) = 0 : Local File ADOSSIER [ADS] : Endif
  #--- Issue 108760 by TS
  #Read [ADS] DOSSIER=APPLI : If fstat Raz [F:ADS] : Endif
  If func DOSSUB.FILL_ADS(APPLI)=0 : End : Endif
  #---
  #--- Issue 108760 by TS
  If filinfo(filpath("FIL","ADOVAL","fde",APPLI),0)<=0 : End : Endif   : # XXXXXX= name of table
  If func DOSVUE.ISVIEW(APPLI,"ADOVAL") : End : Endif                  : # For Archive folder
  #---
  Call MAJ_UTISTO14(APPLI)
End

#######################################################################
# Pour exécution depuis dosmaj (validation dossier)
########################################################################
Subprog UTI_MOUL (APPLI)
Value Char    APPLI

  If APPLI = GDOSX3 Then
    End
  Endif
  #--- Issue 108760 by TS
  If filinfo(filpath("FIL","ADOVAL","fde",APPLI),0)<=0 : End : Endif   : # XXXXXX= name of table
  If func DOSVUE.ISVIEW(APPLI,"ADOVAL") : End : Endif                  : # For Archive folder
  #---
  Call MAJ_UTISTO14(APPLI)
End

